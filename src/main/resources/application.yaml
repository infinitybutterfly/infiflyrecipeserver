ktor:
    application:
        modules:
            - com.infinitybutterfly.ApplicationKt.module
    deployment:
        port: ${PORT:8080}
postgres:
  url: "jdbc:postgresql://localhost/default"
  user: username
  password: passwordjwt
  domain: "https://jwt-provider-domain/"
  audience: "jwt-audience"
  realm: "ktor sample app"
jwt:
    secret: ${JWT_SECRET}
